# URLs des deux Backends UniFlow
VITE_UNIVERSITY_API_URL=https://api-uniflow.kernelforge.codes
# À renseigner uniquement lorsqu’un backend personnel réellement déployé est disponible.
VITE_PERSONAL_API_URL=

# URL générale de fallback ou de l'application Web
VITE_API_URL=https://api-uniflow.kernelforge.codes
VITE_APP_URL=https://uniflow.edu
# Délai maximal des appels API côté navigateur (millisecondes)
VITE_API_TIMEOUT_MS=15000
